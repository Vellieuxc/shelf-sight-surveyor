
import React from "react";
import { StorePicturesSection as PicturesSection } from "../Pictures";

// Re-export the component from the new location with the same props
export default PicturesSection;
